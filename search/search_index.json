{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"about/","title":"About Me","text":""},{"location":"articles/ideas/","title":"Ideas","text":""},{"location":"tutorials/","title":"Tutorials","text":"<p>A collection of tutorials and how-tos related to CoachProxy and other projects.</p> <ul> <li>CoachProxy \u2014 Reflash &amp; Tailscale Setup Guide</li> </ul>"},{"location":"tutorials/creating_pi_image/","title":"Creating a Pi master image (macOS)","text":"<p>This guide shows several ways to create a master Raspberry Pi image on macOS for cloning or backups. Use caution \u2014 imaging tools can overwrite drives.</p>"},{"location":"tutorials/creating_pi_image/#recommended-applepibaker-gui","title":"Recommended: ApplePi\u2011Baker (GUI)","text":"<ul> <li>Download and install ApplePi\u2011Baker v2 (or similar Pi imaging utility).</li> <li>Insert the source microSD/SSD and open the app.</li> <li>Select the source drive and choose Create Backup to produce a <code>.img</code> (or compressed) copy.</li> <li>To restore, select the target drive and use Restore Backup, choosing the saved image file.</li> </ul> <p>ApplePi\u2011Baker provides a simple graphical workflow and verifies operations for you.</p>"},{"location":"tutorials/creating_pi_image/#native-macos-method-terminal-dd","title":"Native macOS method (Terminal \u2014 dd)","text":"<p>Warning: <code>dd</code> is powerful and destructive if given the wrong device. Double-check device IDs before running.</p> <ol> <li>Identify disks:</li> </ol> <pre><code>diskutil list\n</code></pre> <ol> <li>Unmount the source disk (replace <code>&lt;N&gt;</code> with the disk number, e.g. <code>2</code>):</li> </ol> <pre><code>diskutil unmountDisk /dev/disk&lt;N&gt;\n</code></pre> <ol> <li>Create the image (use <code>rdisk</code> for raw speed; replace <code>&lt;N&gt;</code>):</li> </ol> <pre><code>sudo dd if=/dev/rdisk&lt;N&gt; of=~/Desktop/CoachProxy_Master.img bs=1m status=progress\n</code></pre> <p>Notes: - <code>if=</code> is the source device (the Pi drive). - <code>of=</code> is the output file path where the image will be saved. - <code>bs=1m</code> sets a larger block size for faster transfers.</p>"},{"location":"tutorials/creating_pi_image/#alternative-disk-utility","title":"Alternative: Disk Utility","text":"<ul> <li>Open Disk Utility, select the device in the sidebar.</li> <li>Choose File \u2192 New Image \u2192 Image from [Device] and set format to DVD/CD master to capture all sectors.</li> </ul> <p>Disk Utility may produce <code>.dmg</code> files; convert if you need a raw <code>.img</code>.</p>"},{"location":"tutorials/creating_pi_image/#restoring-the-image-to-a-new-drive","title":"Restoring the image to a new drive","text":"<ul> <li>Using ApplePi\u2011Baker: choose the saved image and Restore to the target drive.</li> <li>Using <code>dd</code> (reverse <code>if</code>/<code>of</code>):</li> </ul> <pre><code>sudo dd if=~/Desktop/CoachProxy_Master.img of=/dev/rdisk&lt;TARGET_N&gt; bs=1m status=progress\n</code></pre>"},{"location":"tutorials/creating_pi_image/#tailscale-and-identity-considerations","title":"Tailscale and identity considerations","text":"<p>When cloning a device that has Tailscale configured, clear the old device identity on the first boot of the restored system to avoid duplicate nodes:</p> <pre><code>sudo tailscale logout\n</code></pre> <p>Also consider changing the hostname and SSH keys on the cloned device to avoid conflicts:</p> <pre><code># Change hostname (example)\nsudo hostnamectl set-hostname &lt;new-hostname&gt;\n\n# Regenerate SSH host keys (Debian-based example)\nsudo rm /etc/ssh/ssh_host_* &amp;&amp; sudo dpkg-reconfigure openssh-server\n</code></pre>"},{"location":"tutorials/creating_pi_image/#verification","title":"Verification","text":"<ul> <li>Mount the image or boot the device and confirm system services start as expected.</li> <li>Verify network and Tailscale connectivity after re-authenticating.</li> </ul>"},{"location":"tutorials/creating_pi_image/#safety-tips","title":"Safety tips","text":"<ul> <li>Always confirm device IDs with <code>diskutil list</code> before running destructive commands.</li> <li>Keep at least one verified backup copy off the device you are imaging.</li> <li>Inspect downloaded tools (like ApplePi\u2011Baker) from their official sources.</li> </ul>"},{"location":"tutorials/node-red/","title":"Node-RED: coachproxy Wizard + Dashboard UI (Tutorial)","text":""},{"location":"tutorials/node-red/#purpose","title":"Purpose","text":"<p>This tutorial shows how to access the coachproxy Node-RED wizard, create a Dashboard tab, add a group, and place a Dashboard UI element (using the <code>template</code> node) into that group.</p>"},{"location":"tutorials/node-red/#prerequisites","title":"Prerequisites","text":"<ul> <li>Node-RED is installed and running (typically at <code>http://&lt;host&gt;:1880</code>).</li> <li>The coachproxy integration or nodes are installed in Node-RED (the workspace provides a coachproxy wizard/node).</li> <li>The <code>node-red-dashboard</code> package is installed and enabled.</li> </ul>"},{"location":"tutorials/node-red/#overview","title":"Overview","text":"<ol> <li>Open the Node-RED editor.</li> <li>Launch the coachproxy wizard to configure coach connection/settings.</li> <li>Add a Dashboard tab.</li> <li>Add a Dashboard group inside that tab.</li> <li>Add a <code>template</code> dashboard UI element, assign it to the group, and deploy.</li> </ol>"},{"location":"tutorials/node-red/#steps","title":"Steps","text":""},{"location":"tutorials/node-red/#1-open-the-node-red-editor","title":"1) Open the Node-RED editor","text":"<ul> <li>In a browser, navigate to <code>http://&lt;node-red-host&gt;:1880</code> (replace <code>&lt;node-red-host&gt;</code> with your host or IP).</li> <li>Log in if authentication is enabled.</li> </ul>"},{"location":"tutorials/node-red/#2-launch-the-coachproxy-wizard","title":"2) Launch the coachproxy wizard","text":"<ul> <li>In the Node-RED editor, look for the coachproxy integration. Common places to find it:</li> <li>The right-hand sidebar: some node packages add a sidebar panel named \u201cCoachProxy\u201d or similar.</li> <li>The node palette: type <code>coachproxy</code> in the palette search; a wizard node or helper node may appear.</li> <li>If a dedicated \u201cCoachProxy Wizard\u201d node exists, drag it into a flow and double-click it to open the configuration dialog or wizard button.</li> <li>Follow the prompts to configure connection details (coach host, ports, credentials, and any options the wizard requests). Save/apply the changes.</li> </ul> <p>Notes: If your installation exposes a separate web-based wizard for coachproxy (for example <code>/coachproxy-wizard</code>), open that URL in your browser and follow the same configuration steps.</p>"},{"location":"tutorials/node-red/#3-add-a-dashboard-tab","title":"3) Add a Dashboard Tab","text":"<ul> <li>Ensure the <code>node-red-dashboard</code> sidebar is visible. If not, install/enable <code>node-red-dashboard</code>.</li> <li>In the Dashboard sidebar, click the menu to add a new Tab.</li> <li>Provide a name (for example, <code>CoachProxy</code>) and optional icon.</li> </ul>"},{"location":"tutorials/node-red/#4-add-a-group-to-the-tab","title":"4) Add a Group to the Tab","text":"<ul> <li>In the Dashboard sidebar, under the new Tab, click to add a Group.</li> <li>Configure the Group name (for example, <code>Controls</code>), set width/columns and order as needed.</li> </ul>"},{"location":"tutorials/node-red/#5-add-a-dashboard-ui-template-element-and-assign-it-to-the-group","title":"5) Add a Dashboard UI <code>template</code> element and assign it to the group","text":"<ul> <li>In the Node-RED palette find the <code>template</code> node (it lives under the <code>dashboard</code> category when <code>node-red-dashboard</code> is installed).</li> <li>Drag the <code>template</code> node into your flow.</li> <li>Double-click the <code>template</code> node and set these properties:</li> <li>Name: friendly name like <code>Coach UI Template</code>.</li> <li>Group: select the group you created in step 4 (e.g., <code>Controls / CoachProxy</code>).</li> <li>Template: paste HTML/Angular markup for the element (example below).</li> <li>Size: choose appropriate width/height if needed.</li> <li>Connect the <code>template</code> node to any input nodes you need (e.g., an <code>inject</code> or a function that sends payloads) and click <code>Deploy</code>.</li> </ul> <p>Example <code>template</code> content (simple card showing a value):</p> <pre><code>&lt;div style=\"padding:6px\"&gt;\n  &lt;md-card&gt;\n    &lt;md-card-title&gt;Coach Status&lt;/md-card-title&gt;\n    &lt;md-card-content&gt;\n      &lt;div ng-bind=\"msg.payload.status || 'unknown'\"&gt;&lt;/div&gt;\n    &lt;/md-card-content&gt;\n  &lt;/md-card&gt;\n&lt;/div&gt;\n</code></pre> <p>Example flow snippet (conceptual): - <code>inject</code> node (or incoming msg from coachproxy) -&gt; <code>function</code> (map data to msg.payload) -&gt; <code>template</code> (Dashboard group)</p>"},{"location":"tutorials/node-red/#6-test-and-iterate","title":"6) Test and iterate","text":"<ul> <li>After deploying, open the Dashboard UI (usually <code>http://&lt;node-red-host&gt;:1880/ui</code>) to view the new Tab and Group.</li> <li>Trigger an inject or ensure coachproxy sends data to the flow so the template displays live values.</li> </ul>"},{"location":"tutorials/node-red/#troubleshooting","title":"Troubleshooting","text":"<ul> <li>If you do not see the coachproxy wizard or nodes, verify the coachproxy node package is installed under the Node-RED palette manager.</li> <li>If the Dashboard group is not available in the <code>template</code> node config, restart Node-RED to refresh the UI list and ensure <code>node-red-dashboard</code> is active.</li> <li>Check browser console and Node-RED logs for errors if the template content does not render.</li> </ul>"},{"location":"tutorials/node-red/#tips","title":"Tips","text":"<ul> <li>Use small, focused template elements for easier layout control.</li> <li>Prefer binding to <code>msg.payload</code> for simple data updates; use Angular expressions for more advanced dynamic content.</li> </ul> <p>Created as a concise how-to for adding coachproxy-driven Dashboard UI elements using Node-RED.</p>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/","title":"CoachProxy \u2014 Reflash & Tailscale","text":"<p>tailscale ip -4 tailscale status</p>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/#coachproxy-reflash-tailscale-setup-guide","title":"CoachProxy \u2014 Reflash &amp; Tailscale Setup Guide","text":"<p>This guide walks through flashing a CoachProxy image to a microSD card, preparing legacy Debian Buster repositories (if needed), installing Tailscale, and verifying connectivity.</p>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/#1-flash-the-image","title":"1. Flash the image","text":"<ul> <li>Target: 32GB microSD card</li> <li>Recommended software: balenaEtcher or Raspberry Pi Imager</li> <li>Process: flash your CoachProxy <code>.img</code> to the card, insert it into the Pi, and wait ~2 minutes for the device to complete its initial boot and heartbeat.</li> </ul>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/#2-ssh-into-the-raspberry-pi-local","title":"2. SSH into the Raspberry Pi (local)","text":"<p>Before reflashing or installing software, connect to the Raspberry Pi from a machine on the same local network. Replace the placeholders below with your values \u2014 do not use real credentials here.</p> <pre><code># Basic SSH (replace &lt;username&gt; and &lt;hostname-or-ip&gt; with your values)\nssh &lt;username&gt;@&lt;hostname-or-ip&gt;\n\n# If using an identity file:\nssh -i /path/to/private_key &lt;username&gt;@&lt;hostname-or-ip&gt;\n\n# If the SSH server uses a non-standard port:\nssh -p &lt;port&gt; &lt;username&gt;@&lt;hostname-or-ip&gt;\n</code></pre> <p>When connecting for the first time you may be prompted to accept the host key fingerprint. Authenticate using your account password or your SSH key as configured on the Pi. Ensure you're on a trusted local network when entering credentials.</p>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/#3-update-legacy-repositories-debian-buster","title":"3. Update legacy repositories (Debian Buster)","text":"<p>If the system shows \"No Release file\" errors, point apt to the legacy archive servers. Run this single pasteable block on the Pi (creates backups, applies edits, shows results, and updates apt). You can copy the entire block, paste it into a terminal, and press Enter to run it as one.</p> <pre><code>sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak\nsudo cp /etc/apt/sources.list.d/raspi.list /etc/apt/sources.list.d/raspi.list.bak 2&gt;/dev/null || true\nsudo sed -i 's#raspbian.raspberrypi.org#legacy.raspbian.org#g' /etc/apt/sources.list\nsudo sed -i 's#archive.raspberrypi.org#archive.raspberrypi.org#g' /etc/apt/sources.list.d/raspi.list\necho \"----- /etc/apt/sources.list -----\"\nsudo cat /etc/apt/sources.list\necho \"----- /etc/apt/sources.list.d/raspi.list -----\"\nsudo cat /etc/apt/sources.list.d/raspi.list\nsudo apt update\n</code></pre>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/#4-install-tailscale","title":"4. Install Tailscale","text":"<p>Run the official Tailscale install script:</p> <pre><code>curl -fsSL https://tailscale.com/install.sh | sh\n</code></pre>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/#5-authenticate-and-enable-ssh","title":"5. Authenticate and enable SSH","text":"<p>Start Tailscale and authenticate the device:</p> <pre><code>sudo tailscale up\n</code></pre> <p>When prompted, copy the URL printed to the terminal, open it in a browser, and sign in to authorize the Pi.</p> <p>Enable Tailscale SSH (optional):</p> <pre><code>sudo tailscale set --ssh\n</code></pre>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/#6-tailscale-admin-console-and-sharing","title":"6. Tailscale admin console and sharing","text":"<ul> <li>Open the Tailscale Admin Console to view devices.</li> <li>Find your Pi (often <code>raspberrypi</code> or <code>coachproxy</code>) and confirm the status dot is green.</li> <li>Verify that <code>SSH</code> appears under the device Services.</li> </ul> <p>If a device shows \"Expired\" or \"Needs Re-authentication\", use the three-dot menu next to the device in the console and choose Disable Key Expiry to avoid future lockouts.</p> <p>To share access with another Tailscale user:</p> <ol> <li>Click Share... on the device row.</li> <li>Generate a share link and send it to the recipient. They can then access the CoachProxy Web UI via the device's Tailscale IP while the link is active.</li> </ol>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/#7-local-verification","title":"7. Local verification","text":"<p>Run the following on the Pi to view the assigned Tailscale IP and connection status:</p> <pre><code>tailscale ip -4\ntailscale status\n</code></pre>"},{"location":"tutorials/reflash_coachproxy_add_tailscale/#notes","title":"Notes","text":"<ul> <li>Commands in this guide require <code>sudo</code> and network access. Use caution when running commands that modify system files.</li> <li>This guide focuses on Debian Buster / legacy setups; adapt repository lines as needed for other distributions or releases.</li> </ul>"},{"location":"tutorials/shutdown_coachproxy/","title":"Shutdown CoachProxy safely","text":"<p>Performing a proper software shutdown prevents SD card corruption and reduces the risk of boot problems. This guide lists safe shutdown methods and what to do in emergency situations.</p>"},{"location":"tutorials/shutdown_coachproxy/#recommended-use-the-coachproxy-ui-preferred","title":"Recommended: use the CoachProxy UI (preferred)","text":"<p>If the CoachProxy web UI is available, use its System \u2192 Shutdown or Reboot option. This performs a controlled shutdown that is safest for the device and storage. Use the UI first when possible; the steps below remain available if the UI is unreachable.</p>"},{"location":"tutorials/shutdown_coachproxy/#command-line-shutdown-fallback","title":"Command line shutdown (fallback)","text":"<p>If the UI is unavailable, connect locally or via Tailscale SSH and run:</p> <pre><code>sudo shutdown -h now\n</code></pre> <p>Wait for the system activity LED (the green \"ACT\" light on Raspberry Pi models) to stop blinking and turn off before removing power.</p>"},{"location":"tutorials/shutdown_coachproxy/#physically-removing-power","title":"Physically removing power","text":"<ul> <li>After a confirmed software shutdown (UI or CLI) and when LEDs indicate the device is powered down, you may unplug the power connector.</li> <li>Use gentle, steady force when disconnecting any clip-style connectors.</li> </ul>"},{"location":"tutorials/shutdown_coachproxy/#emergency-unresponsive-device","title":"Emergency / unresponsive device","text":"<p>If the system is completely unresponsive and you cannot access the UI or SSH, pulling the power may be unavoidable. After an emergency unplug:</p> <ul> <li>Inspect the storage (SD card) for filesystem errors on the next boot and restore from backups if needed.</li> <li>Consider running a filesystem check on another machine or mounting the card to verify integrity.</li> </ul>"},{"location":"tutorials/shutdown_coachproxy/#tips","title":"Tips","text":"<ul> <li>Always keep recent backups or a master image handy for quick recovery.</li> <li>Consider using an external UPS if the device is in a location prone to power interruptions.</li> </ul>"}]}